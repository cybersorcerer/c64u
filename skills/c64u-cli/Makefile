# Verify the reference against the CLI it documents.
#
#   make check   every command in references/commands.md still exists
#
# There is nothing to build here; the skill is documentation. What can rot is
# the command list, so that is what gets checked.

.PHONY: check

check:
	@sh check.sh
